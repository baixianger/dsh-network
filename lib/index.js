/** The transport-neutral network contract revision. */
export const DSH_NETWORK_PROTOCOL_VERSION = 1;

/** The preview package deliberately ships contracts before a concrete transport. */
export const DSH_NETWORK_STAGE = "design-preview";
